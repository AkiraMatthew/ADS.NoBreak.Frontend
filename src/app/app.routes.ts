import { Routes } from '@angular/router';

export const routes: Routes = [
    {
        path: 'login',
        component: LoginPage
    }
    {
        path: 'register',
        component: RegisterPage
    }
];
